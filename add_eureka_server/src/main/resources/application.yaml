server:
  port: 10086   #默认值为8761

Spring:
  application:
    name: eureka-server  #配置服务的名称

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8848/eureka  #配置Eureka服务地址
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1   #配置服务器地址(从该地址拉取,注册)